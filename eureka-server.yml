
# Con spring cloud e config server xa non fai falta indicar o nome de aplicacion, pero debemos indicar o nome do archivo igual que o nome de aplicacion
#spring:
#  application:
#    name: eureka-server
# NON vale a barra baixa... NON funcionaría con eureka_server

server:
  port: 8761
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url: 
    default-zone: http://localhost:8761/eureka
  server:
    wait-time-in-ms-when-sync-empty: 0
    
    
